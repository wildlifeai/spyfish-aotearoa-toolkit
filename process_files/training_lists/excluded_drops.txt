# Drops to exclude from training.
#
# Used by spyfish.ml.training.prepare_training_data to filter out DropIDs
# *before* ceiling/floor balancing and train/val/test splitting. Excluded
# drops are invisible to the rest of the pipeline — they contribute zero
# frames, and their species counts don't influence the ceiling math.
#
# Format:
#   - One DropID per line
#   - Blank lines ignored
#   - Lines starting with '#' are comments
#   - Inline comments allowed after '#' on the same line
#
# Example:
#   LDN_20220513_BUV_WWS_001_01   # QA still in progress
#   ABC_20230101_BUV_XYZ_002_01   # waiting on expert re-annotation
#   DEF_20240215_BUV_LNM_003_01   # bad lighting — flagged 2026-03-10
#
# This file is the canonical list — edit it here and commit. Not in config.yaml.

# --- Excluded DropIDs ---

# Local frames are 1440×1080 but Biigle annotators saw the source at ~1920×1080
# (audit 2026-04-26: max annotation x reaches ~1934, ~34% over local width).
# This is a horizontal crop — coordinates can't be linearly remapped, so labels
# don't align to local image content. Re-pull Biigle source frames to recover.
# TON_20211026_BUV_TON_050_01   # dim mismatch: local 1440×1080 vs Biigle 1920×1080
# TON_20211026_BUV_TON_053_01   # dim mismatch: local 1440×1080 vs Biigle 1920×1080
# TON_20211026_BUV_TON_071_01   # dim mismatch: local 1440×1080 vs Biigle 1920×1080
# UoA legacy BUV (Underwood & Jeffs, Jan 2022), not Spyfish deployments: different
# sites, rig, and label vocabulary, and the filenames are UoA-format
# (ESK_X_B_20220118_PM_v003_f030900_t000515.51s.jpg), not DropIDs. They belong to
# the UoA ML->Biigle workflow, not to the Spyfish species model.
# 30184 leaked 103 frames (53 MOT + 50 ESK) into the 2026-08-22 training set.
volume_30196   # 348 UoA-named frames
volume_30184   # 287 UoA-named frames (169 ESK + 118 MOT)
